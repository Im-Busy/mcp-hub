# Build & Test Scripts

Place project-specific build, test, and utility scripts here.

## Conventions

- Shell scripts use `.ps1` extension (PowerShell on Windows)
- All scripts must be runnable from the project root
- Exit code 0 = success, non-zero = failure
